---
import ClubBase from '../layouts/ClubBase.astro';
import {
  PRICE_TO_BE_CONFIRMED,
  comparisonText,
  parseLinks,
  parseMembership,
  parsePartners,
} from '@src/shared/club-content';
import { formatPriceWords } from '@src/shared/money';
import links from '../content/links.json';
import membership from '../content/membership.json';
import partners from '../content/partners.json';

// `/membership/` — what it costs to run with the club, and what joining adds.
//
// ## Every action leaves for the old site, and no form is built here
//
// ⚠️ **Nothing on this page collects anything.** The mockup carries a three-step join form
// asking for date of birth, address and phone number — all personal data, which is a committee
// decision and a privacy-notice change rather than a build task. Join, renew, cancel, pay and
// the WhatsApp request are all links, and every one of their addresses comes from
// `links.json`, never from this markup.
//
// **That is what makes the cutover a data change.** When the club's own join page is built,
// changing `href` and `where` on one key moves this page, the label included.
//
// ## ⚠️ Two of the four prices are painted by the Worker, not rendered here
//
// **Annual membership and the England Athletics option are read from the database on every
// request**, by `worker/membership.ts`, because the club asked that a fee rise be one SQL
// statement rather than a deploy. What ships in the markup is `PRICE_TO_BE_CONFIRMED`; the
// Worker replaces it through the `data-membership-price` attribute on each slot.
//
// So **the figure you are looking for is not in this file and must never be added to it.** A
// literal here would be a second source that agreed on the day it was typed and stopped
// agreeing the day somebody ran the `update` — the failure `entries.fees` exists to rule out.
// `parseMembership()` no longer has the keys, so this is enforced rather than remembered.
//
// **When nothing is painted the page says the price is confirmed on the form.** That is the
// shipped state and it is the safe one: a page that cannot reach the database must not quote
// a figure somebody would act on. The card still links to Join either way, because it is the
// price that is unknown and not the route.
//
// ## Money goes through `formatPriceWords()`
//
// "Run for 50p" is the club's voice. `formatPence()` renders that as "£0.50", which is a
// different sentence — so this page uses the sibling, and neither writes its own `£`. The
// Worker paints through the same function for the same reason.
//
// ## Club kit is out of scope
//
// **Deliberately absent**: no kit section, no order window, no stock table, no collection
// point. It comes back as its own piece of work. There is no `kit-order` key in `links.json`
// either, because an unused address sitting in that file is how it quietly returns.
//
// ## The partner filter is not here yet
//
// A progressive enhancement, landing in the next change. Every partner is listed; the filter
// only ever hides. With scripting off nothing is lost.

const prices = parseMembership(membership);
const { partners: businesses } = parsePartners(partners);
const link = parseLinks(links);

const perRun = formatPriceWords(prices.payPerRunPence);
const perMonth = formatPriceWords(prices.subscriptionPerMonthPence);

const { comparison, licence } = prices;

/** What a cell's `{perRun}` and `{perMonth}` tokens resolve to. One source, four cells. */
const cellPrices = { perRun, perMonth };

/**
 * The four options, in the table's column order, which is `cells`' order.
 *
 * ⚠️ **These strings are the column headings *and* each cell's `data-label`.** Below 48em the
 * table stacks and the headings go, so `::before` reads the label off the cell — and a cell
 * labelled differently from the column it came from would be a lie nobody could see, because
 * the two presentations are never on screen at once. One list is what stops that.
 */
const COLUMNS = [
  'Pay as you run',
  'Unlimited runs',
  'SRC membership',
  'SRC + EA',
] as const;

/**
 * England Athletics' registration year, as a sentence.
 *
 * ⚠️ **Composed here rather than interpolated into the markup**, because three values inside
 * one sentence is three chances to meet the trap this repository has already paid for five
 * times: Prettier puts a newline between a word and the expression after it, and Astro
 * compresses that newline to nothing — `renewed by30 June`. A single expression cannot be
 * glued to anything.
 */
const licenceTerms =
  `England Athletics registration runs ${licence.yearStarts} to ` +
  `${licence.yearEnds} and must be renewed by ${licence.renewBy}. Benefits are as ` +
  `published by England Athletics and may change.`;
---

<ClubBase
  title="Membership — Southville Running Club"
  description="Run with Southville Running Club for 50p, subscribe for unlimited runs, or join the club for the year. What each option includes, and the England Athletics licence."
  canonical="https://new.southvillerunningclub.co.uk/membership/"
>
  <header class="club-phead">
    <div class="club-wrap">
      {
        /* ⚠️ **The annual price is not in this heading, and that is deliberate.** It read
        "Run for 50p. Join for £4." — good copy, and a second place a price would have to be
        changed. A painted slot in a headline reads as a gap whenever the database is
        unreachable, so the heading says the half that is settled in this repository and the
        cards carry the half that is not. */
      }
      <h1>Run for {perRun}. Join when it suits you.</h1>
      <p class="club-lede">
        You don't need to be a member to run with us. Pay as you go, subscribe, or join
        the club: whatever suits you.
      </p>
    </div>
  </header>

  <section class="club-sec">
    <div class="club-wrap">
      {
        /* An `h2` because the page already has an `h1`, and the lede sits under it rather
        than in the page header: the header's lede is about not needing to be a member at
        all, and this one is about which of the four to pick. */
      }
      <h2>Run with us, join us, or race with us</h2>
      {
        /* ⚠️ **The annual price in this lede is painted, exactly as the home page's heading
        is.** "Join for £4" is a literal the built file may not contain — `membership.test.ts`
        asserts that of every page — so the slot ships `PRICE_TO_BE_CONFIRMED` and the Worker
        fills it. Unpainted the sentence is clumsier and still true, which is the trade `/`
        already makes; the cards below say it again in their own right. */
      }
      <p class="club-lede">
        Pay per run or subscribe to cover the hall. Join for <span
          data-membership-price="club">{PRICE_TO_BE_CONFIRMED}</span
        > to support the club. Add the England Athletics licence if you race: cheaper entries,
        insurance and partner discounts usually pay it back within the year.
      </p>

      <div class="club-g4 club-pricing">
        <article class="club-card">
          <h3>Pay as you run</h3>
          <p class="club-price">{perRun}<small> a run</small></p>
          <p>
            Covers hire of the Southbank Club. Trust-based: pop it in the pot or pay
            online.
          </p>
          <a class="club-btn club-btn-secondary" href={link['pay-per-run'].href}>
            {link['pay-per-run'].label} on our old site
          </a>
        </article>

        <article class="club-card">
          <h3>Unlimited runs</h3>
          <p class="club-price">{perMonth}<small> a month</small></p>
          <p>
            A subscription, not membership. Start or stop any time. Track sessions are
            paid separately. Non-refundable.
          </p>
          <a class="club-btn club-btn-secondary" href={link.subscribe.href}>
            {link.subscribe.label} on our old site
          </a>
        </article>

        <article class="club-card club-featured">
          {/* The outline is the non-text half; this says it in words. */}
          <p class="club-badge">Most people start here</p>
          <h3>SRC membership</h3>
          {
            /* Painted from `membership.membership_types` where `code = 'club'`. The shipped
            text is what a reader sees if the Worker could not ask. */
          }
          <p class="club-price">
            <span data-membership-price="club">{PRICE_TO_BE_CONFIRMED}</span>
            <small> a year</small>
          </p>
          <p>
            Supports the club, and gets you into the members' WhatsApp community and local
            discounts.
          </p>
          {
            /* ⚠️ **Outlined, and the filled one is now the card beside it.** Both cards are
            a membership and only one can be the loudest; the licence card is where the club
            points somebody who races. This still reads "Most people start here" in words. */
          }
          <a class="club-btn club-btn-secondary" href={link.join.href}>
            {link.join.label}
          </a>
        </article>

        <article class="club-card">
          {/* Same pill as the card beside it: the badge is the words, not a colour. */}
          <p class="club-badge">Best value if you race</p>
          <h3>SRC + England Athletics licence</h3>
          {
            /* ⚠️ **Never a guess, and never a literal.** Painted from `code = 'club_ea'`;
              the split beneath it is painted too, and ships `hidden` because there is
              nothing honest to say about how £27 divides until the row has been read.

              `data-membership-ea-year` is read by the same handler and appended — the money
              is the database's and the registration year is `membership.json`'s, so neither
              is written here and the sentence still arrives as one phrase. */
          }
          <p class="club-price">
            <span data-membership-price="club_ea">{PRICE_TO_BE_CONFIRMED}</span>
            <small> a year</small>
          </p>
          <p
            class="club-small"
            data-membership-ea-split="club_ea"
            data-membership-ea-year={licence.year}
            hidden
          >
          </p>
          <p>
            Everything in membership, plus £2 off every licensed race, race insurance, the
            London Marathon club ballot and the EA Rewards Hub.
          </p>
          {
            /* ⚠️ **The licence section keeps an inbound link, because the button no longer
            is one.** This card's button used to be the only route to `#england-athletics`.
            A small link keeps it, and the button below it stays the card's last element so
            `.club-pricing`'s `margin-top: auto` still lines the four up. */
          }
          <p class="club-small">
            <a href="#england-athletics">What the licence covers</a>
          </p>
          {
            /* ⚠️ **Not "on our old site", and not an invented address.** Joining with a
            licence is the club's own form — it is what `membership_types`' `club_ea` row is
            for — so this is `links.json`'s `join` key, which is on this site. A button
            claiming to leave while staying is worse than either, and `club-pages.spec.ts`
            asserts that in as many words. */
          }
          <a class="club-btn club-btn-primary" href={link.join.href}>
            Join with a licence
          </a>
        </article>
      </div>

      {
        /* ## The comparison table
          
          ⚠️ **Every row and every group is in `membership.json`, and removing one removes it
          from the page.** England Athletics republishes its benefit list each year and the
          club does not control it, so the alternative is a template somebody edits annually.
          Nothing here names a benefit.

          **A `<tbody>` per group**, which is what a row group actually is, with the group's
          name as a `<th colspan="5">` above it. The heading is a real table header rather
          than a styled `<td>`, so "Racing" is announced as a heading over the rows beneath
          it instead of as an empty cell.

          **Below 48em the whole thing stacks** — five columns cannot be read at 320px — and
          each cell carries its column's name in `data-label` for `::before` to draw. The
          headings go with `display: none` at that width, so the label is what associates a
          cell with its option; that is why `COLUMNS` supplies both and neither is typed
          twice. */
      }
      <div
        class="club-table-wrap"
        tabindex="0"
        role="region"
        aria-label="What each option includes"
      >
        <table class="club-table club-table-compare club-table-grouped">
          <caption>What each option includes</caption>
          <thead>
            <tr>
              <th scope="col"><span class="club-visually-hidden">Benefit</span></th>
              {COLUMNS.map((column) => <th scope="col">{column}</th>)}
            </tr>
          </thead>
          {
            comparison.map((group) => (
              <tbody>
                <tr class="club-row-group">
                  {/* Five, because the row header counts as a column. */}
                  <th scope="colgroup" colspan="5">
                    {group.group}
                  </th>
                </tr>
                {group.rows.map((row) => (
                  <tr>
                    <th scope="row">
                      {row.benefit}
                      {/* ⚠️ A `<span>` inside the row header, not a second row: a supporting
                        line in a row of its own would have four empty cells beside it, and
                        would be read out as a row somebody has to work out the shape of. */}
                      {row.note !== undefined && (
                        <span class="club-row-note">{row.note}</span>
                      )}
                    </th>
                    {row.cells.map((cell, column) =>
                      typeof cell === 'string' ? (
                        <td data-label={COLUMNS[column]}>
                          {comparisonText(cell, cellPrices)}
                        </td>
                      ) : (
                        <td data-label={COLUMNS[column]}>
                          {/* ⚠️ A glyph is not a word, and colour is never the signal. The
                            tick is drawn rather than typed — an inline SVG takes the current
                            ink, so it is legible in both schemes without a colour of its own
                            — and each cell carries its own text, so the table means the same
                            thing read aloud as it does read across. */}
                          {cell ? (
                            <svg
                              class="club-tick"
                              viewBox="0 0 16 16"
                              width="16"
                              height="16"
                              aria-hidden="true"
                              focusable="false"
                            >
                              <path
                                d="M2 8.5 6 12.5 14 3.5"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2.5"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                              />
                            </svg>
                          ) : (
                            <span aria-hidden="true">–</span>
                          )}
                          <span class="club-visually-hidden">
                            {cell ? 'Included' : 'Not included'}
                          </span>
                        </td>
                      ),
                    )}
                  </tr>
                ))}
              </tbody>
            ))
          }
        </table>
      </div>

      <p class="club-small">
        * Or add the {perMonth} monthly subscription for unlimited runs.
      </p>

      {
        /* ⚠️ **The England Athletics fee is a sentence of its own, and it ships hidden.**
          The £23 is `membership_types.ea_fee_pence` and no built page may contain it, so
          this is painted like the split line in the card — the registration year comes off
          `data-membership-ea-year` and the money out of the database. Unpainted, the page
          simply does not make the claim, which is the direction every other failure here
          takes. */
      }
      <p
        class="club-small"
        data-membership-ea-fee="club_ea"
        data-membership-ea-year={licence.year}
        hidden
      >
      </p>

      <p class="club-small">{licenceTerms}</p>

      {
        /* ⚠️ **Absent until the committee agrees the wording.** Second-claim membership is
          offered here as a sentence the club supplied and the committee has not signed off;
          an absent key publishes nothing, which is what "not agreed yet" should look like.
          Plain text rather than a link, because the route that exists — the Membership
          Officer, through the committee page — is in the licence section below and
          `links.json` has no second-claim address to invent one from. */
      }
      {
        licence.secondClaim !== undefined && (
          <p class="club-small">{licence.secondClaim}</p>
        )
      }
    </div>
  </section>

  <section class="club-sec club-sec-alt" id="england-athletics">
    <div class="club-wrap club-g2 club-split">
      <div>
        <h2>The England Athletics licence</h2>
        <ul class="club-ticks">
          <li>
            Discounted entry to affiliated races. It pays for itself after about nine
            races.
          </li>
          <li>Annual race insurance.</li>
          <li>You need SRC membership first.</li>
          <li>
            Renews every April. First-time applicants from January to March get up to
            three months free.
          </li>
          <li>
            Never enter at the affiliated rate without a valid licence, and wear club
            green when you do.
          </li>
        </ul>
      </div>

      <div class="club-card">
        <h3>Join, renew or leave</h3>
        <ul class="club-actions">
          <li>
            <a href={link.join.href}>{link.join.label}</a>
            <span>New members. Takes about five minutes.</span>
          </li>
          <li>
            <a href={link.renew.href}>{link.renew.label}</a>
            <span>
              Through the England Athletics portal, after agreeing to club policies. On
              our old site.
            </span>
          </li>
          <li>
            <a href={link.cancel.href}>{link.cancel.label}</a>
            <span>Moving, changing clubs or stopping? Let us know, on our old site.</span>
          </li>
        </ul>
        {
          /* ⚠️ No `membership@…` address: the club has not confirmed one. The committee page
            is the route that exists. */
        }
        <p class="club-small">
          Questions? <a href={link.committee.href}>Ask the Membership Officer</a> through the
          committee page on our old site.
        </p>
      </div>
    </div>
  </section>

  <section class="club-sec" id="discounts">
    <div class="club-wrap">
      <h2>Member discounts</h2>
      <p>
        Local businesses that look after SRC members. Codes are shared in the members'
        WhatsApp community, never on this page.
      </p>

      <ul class="club-g3">
        {
          businesses.map((partner) => (
            <li class="club-card club-partner">
              <h3>{partner.name}</h3>
              <p class="club-small">{partner.category}</p>
              {/* Absent for every partner today — see this file's header. */}
              {partner.offer !== undefined && <p>{partner.offer}</p>}
              <p class="club-locked">Code in the members' WhatsApp</p>
            </li>
          ))
        }
      </ul>

      <p class="club-small">
        Each partner's offer is being confirmed with them before it is published here.
      </p>
    </div>
  </section>

  <section class="club-sec club-sec-alt">
    <div class="club-wrap club-g2 club-split">
      <div>
        <h2>Members' WhatsApp community</h2>
        <p>
          Club news, lifts to races, lost property and the discount codes. For members
          only, so we'll check your SRC membership before adding you.
        </p>
        <p>
          <a class="club-btn club-btn-primary" href={link.whatsapp.href}>
            {link.whatsapp.label} on our old site
          </a>
        </p>
      </div>

      <details class="club-card club-inline">
        <summary>The community's 12 rules</summary>
        <ol>
          <li>Be courteous and kind.</li>
          <li>Respect other people's views.</li>
          <li>No bad language.</li>
          <li>No inappropriate images.</li>
          <li>Avoid politics and religion.</li>
          <li>No spam or advertising without admin approval.</li>
          <li>No harassment or bullying.</li>
          <li>No unsolicited direct messages to other members.</li>
          <li>Raise any issues with the admins, not in the group.</li>
          <li>Keep it relevant to running and the club.</li>
          <li>Breaches can mean a temporary ban.</li>
          <li>Serious or repeated breaches mean removal.</li>
        </ol>
      </details>
    </div>
  </section>
</ClubBase>
